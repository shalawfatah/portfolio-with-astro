---
import teaCup from '../../images/teacup.png'
import smoke from '../../images/fog.gif'
---
<div class="h-screen w-screen hero-container">
    <div class="text text-left">
        <h1 class='hero-intro'><span>👋</span> I'm Shalaw, a web developer <br/>  focusing on <span class="text-transparent bg-clip-text bg-gradient-to-r from-indigo-500 via-purple-500 to-pink-500">frontend</span>
            <span class="text-transparent bg-clip-text bg-gradient-to-r from-gray-200 via-gray-300 to-gray-200">&</span>
            <span class="text-transparent bg-clip-text bg-gradient-to-r from-[#44190B] via-[#b98754] to-[#6f502a]">tea</span>
        </h1>
    </div>
    <div class="images relative">
        <img class="h-auto w-auto absolute -top-48 opacity-25" src={smoke} alt="tea cup" />
        <img class="tea-cup" src={teaCup} alt="tea cup" />
    </div>
</div>

<style>
    .hero-container {
        display: flex;
        flex-wrap: wrap;
        height: 90vh;
        align-items: center;
        justify-content: center;
        padding-right: 50px;
        padding-left: 50px;
    }
    .home-hero {
        height: 92vh;
    }
    .hero-intro {
        font-size: 40px;
        text-align: center;
        font-weight: bold;
    }
    .tea-cup {
        width: 400px;
    }
    @media only screen and (max-width: 900px) {
        .hero-intro {
        font-size: 26px;
        }
    .hero-container {
        /* display: flex; */
        /* flex-wrap: wrap; */
        height: 90vh;
        /* align-items: center; */
        /* justify-content: center; */
    }
    }
</style>